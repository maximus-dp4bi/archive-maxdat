-- add columns to both S_METRIC_TEMPLATE and F_METRIC to support checkbox input of NOT_SUPPLIED column in metric template
ALTER TABLE S_METRIC_TEMPLATE ADD (
  ACTUAL_VALUE_NOT_SUPPLIED VARCHAR(1)
  , FORECAST_VALUE_NOT_SUPPLIED VARCHAR(1)
);

ALTER TABLE F_METRIC ADD (
  ACTUAL_VALUE_NOT_SUPPLIED VARCHAR(1)
  , FORECAST_VALUE_NOT_SUPPLIED VARCHAR(1)
);

CREATE OR REPLACE VIEW F_METRIC_SV AS
SELECT F_METRIC.* FROM F_METRIC;

INSERT INTO CC_L_PATCH_LOG ( PATCH_VERSION , SCRIPT_SEQUENCE , SCRIPT_NAME) 
VALUES ('2.8','007','007_ALTER_S_METRIC_TEMPLATE_AND_F_METRIC');

COMMIT;