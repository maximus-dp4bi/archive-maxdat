CREATE_TABLES:
- contains the DDL necessary to build the stub database

CREATE_USER:
- contains the DDL necessary to create the user for the stub database

---

The SQL and .csv files present in this directory contain the data to 
load the stub tables in the database.  The .csv serves as an alternative
to the SQL file where there may be too many rows to open it in SQLDeveloper.
