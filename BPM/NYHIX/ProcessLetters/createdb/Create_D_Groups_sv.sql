---- NYHIX-33561 
CREATE or REPLACE VIEW D_GROUPS_SV
AS SELECT 
GROUP_ID, 
GROUP_NAME,
DESCRIPTION,
PARENT_GROUP_ID,
START_DATE,
END_DATE,
TYPE_CD,
CREATE_TS,
UPDATE_TS
FROM MAXDAT.GROUPS_STG WITH READ ONLY;

grant select on D_GROUPS_SV to MAXDAT_READ_ONLY;