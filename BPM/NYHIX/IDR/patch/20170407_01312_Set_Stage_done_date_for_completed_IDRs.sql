UPDATE NYHX_ETL_IDR_INCIDENTS
SET STAGE_DONE_DATE = SYSDATE
, STG_LAST_UPDATE_DATE = SYSDATE
, CURRENT_STEP = 'Closed'
WHERE stage_done_date IS NULL
AND ADD_MONTHS(TRUNC(complete_dt),3) <= TRUNC(sysdate)
AND COMPLETE_DT IS NOT NULL;

commit;
