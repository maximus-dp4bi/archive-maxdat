CREATE TABLE TX_LETTERS_BAD_ADDRESS
(job_id   NUMBER(18,0),
row_num   NUMBER(18,0),
prog_type VARCHAR2(30),
population VARCHAR2(100),
srvc_areacode VARCHAR2(20),
ltr_type  VARCHAR2(10),
lmreq_id  NUMBER(18,0),
case_cin  VARCHAR2(30),
ltr_date  DATE,
return_date DATE,
status_code VARCHAR2(25),
error_code  VARCHAR2(25),
ncoa_code   VARCHAR2(25),
moved       VARCHAR2(5),
move_date   DATE,
processed_ind NUMBER(1,0),
error_count NUMBER(8,0),
error_fields VARCHAR2(2000),
bad_address_type VARCHAR2(25),
filename VARCHAR2(30)) TABLESPACE MAXDAT_DATA;


GRANT SELECT ON TX_LETTERS_BAD_ADDRESS TO MAXDAT_READ_ONLY;

CREATE INDEX LTR_BADADDRESS_IDX1 on TX_LETTERS_BAD_ADDRESS (LMREQ_ID) TABLESPACE MAXDAT_INDX;
CREATE UNIQUE INDEX LTR_BADADDRESS_UX1 on TX_LETTERS_BAD_ADDRESS (JOB_ID,ROW_NUM) TABLESPACE MAXDAT_INDX;

create or replace view D_PL_BAD_ADDRESS_SV as
select * 
from TX_LETTERS_BAD_ADDRESS
with read only;


grant select on D_PL_BAD_ADDRESS_SV to MAXDAT_READ_ONLY;