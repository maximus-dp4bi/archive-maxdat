

alter table D_COMPLAINT_CURRENT add(CURRENT_STEP varchar(256));

alter table NYHX_ETL_COMPLAINTS_INCIDENTS add (CURRENT_STEP varchar(256));
alter table NYHX_ETL_COMPL_INCIDENTS_OLTP add (CURRENT_STEP varchar(256));
alter table NYHX_ETL_COMPL_INCIDN_WIP_BPM add (CURRENT_STEP varchar(256));


CREATE OR REPLACE VIEW D_COMPLAINT_CURRENT_SV AS
SELECT * FROM D_COMPLAINT_CURRENT
WITH READ ONLY;

commit;