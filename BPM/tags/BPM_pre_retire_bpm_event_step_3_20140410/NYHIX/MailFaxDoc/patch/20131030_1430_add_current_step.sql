

alter table D_NYHIX_MFD_CURRENT add(CURRENT_STEP varchar(256));

alter table NYHIX_ETL_MAIL_FAX_DOC add (CURRENT_STEP varchar(256));
alter table NYHIX_ETL_MAIL_FAX_DOC_OLTP add (CURRENT_STEP varchar(256));
alter table NYHIX_ETL_MAIL_FAX_DOC_WIP_BPM add (CURRENT_STEP varchar(256));


CREATE OR REPLACE VIEW D_NYHIX_MFD_CURRENT_SV AS
SELECT * FROM D_NYHIX_MFD_CURRENT
WITH READ ONLY;

commit;